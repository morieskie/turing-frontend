import {Component, Input, OnInit} from '@angular/core';

@Component({
  selector: 'app-catalogue-toolbar',
  templateUrl: './template/catalogue-toolbar.component.html',
  styleUrls: [
  ],
})
export class CatalogueToolbarComponent implements OnInit {
  constructor() {
  }

  ngOnInit(): void {
  }
}
